<form method="post" action="{{ path('valeur_delete', {'idValeur': valeur.idValeur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valeur.idValeur) }}">
    <button class="btn">Delete</button>
</form>
