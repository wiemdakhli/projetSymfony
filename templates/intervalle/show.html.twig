{% extends 'base.html.twig' %}

{% block title %}Intervalle{% endblock %}

{% block body %}
    <h1>Intervalle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervalle.idIntervalle }}</td>
            </tr>
            <tr>
                <th>Rang min</th>
                <td>{{ intervalle.rangmin }}</td>
            </tr>
            <tr>
                <th>Rang max</th>
                <td>{{ intervalle.rangmax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('intervalle_index') }}">retour à la liste</a>

    <a href="{{ path('intervalle_edit', {'idIntervalle': intervalle.idIntervalle}) }}">modifier</a>

    {{ include('intervalle/_delete_form.html.twig') }}
{% endblock %}
