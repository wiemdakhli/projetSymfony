{% extends 'base.html.twig' %}

{% block title %}Intervalle{% endblock %}

{% block body %}
<div class="container" align="center">
    <div class="col-4">
    <center><h1>{{ intervalle.idIntervalle }}</h1></center>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th class="table-dark">Id</th>
                <td class="table-info">{{ intervalle.idIntervalle }}</td>
            </tr>
            <tr>
                <th class="table-dark">Rang min</th>
                <td class="table-info">{{ intervalle.rangmin }}</td>
            </tr>
            <tr>
                <th class="table-dark">Rang max</th>
                <td class="table-info">{{ intervalle.rangmax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('intervalle_index') }}">retour à la liste</a>

    <!-- <a href="{{ path('intervalle_edit', {'idIntervalle': intervalle.idIntervalle}) }}">modifier</a> -->

    {{ include('intervalle/_delete_form.html.twig') }}
</div>
</div>
{% endblock %}
