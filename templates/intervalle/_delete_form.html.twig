<form method="post" action="{{ path('intervalle_delete', {'idIntervalle': intervalle.idIntervalle}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intervalle.idIntervalle) }}">
    <button class="btn">Supprimer</button>
</form>
