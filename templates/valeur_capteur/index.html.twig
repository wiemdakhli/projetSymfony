{% extends 'base.html.twig' %}

{% block title %}ValeurCapteur index{% endblock %}

{% block body %}
    <h1>ValeurCapteur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdValCap</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for valeur_capteur in valeur_capteurs %}
            <tr>
                <td>{{ valeur_capteur.idValCap }}</td>
                <td>
                    <a href="{{ path('valeur_capteur_show', {'idValCap': valeur_capteur.idValCap}) }}">show</a>
                    <a href="{{ path('valeur_capteur_edit', {'idValCap': valeur_capteur.idValCap}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('valeur_capteur_new') }}">Create new</a>
{% endblock %}
