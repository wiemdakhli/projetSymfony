<form method="post" action="{{ path('valeur_capteur_delete', {'idValCap': valeur_capteur.idValCap}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ valeur_capteur.idValCap) }}">
    <button class="btn">Delete</button>
</form>
