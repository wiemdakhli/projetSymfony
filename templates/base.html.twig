<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <link rel="stylesheet" href="https://bootswatch.com/5/lux/bootstrap.min.css">
        <!-- CSS only -->
        <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous"> -->
        <!-- JavaScript Bundle with Popper -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous" defer></script>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
        
        {% block stylesheets %}
        <style>
            label{font-weight:bold;font-size:20px;}
            table,th,td{font-weight:bold;}
            i{font-size:20px;}
            body{background-color:white;}
        </style>
        
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark" >
            <div class="container-fluid" >
              <a style ="background-color:white;" class="navbar-brand" href="#">
                <img width="150px" src="{{asset('assets/images/logo.png')}}" alt="">
              </a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarColor02" >
                <ul class="navbar-nav me-auto" style="font-size: 16px;">
                  {% if is_granted('ROLE_ADMIN') %}
                  <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_index')}}" style="color:white;">admin</a>
                  </li>
                  {% endif %}
                  <li class="nav-item">
                    <a class="nav-link" href="{{ path('valeur_index') }}" style="color:white;">historique</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="{{ path('capteur_index') }}" style="color:white;">device</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" href="http://www.dig2s.com/" style="color:white;">a propos</a>
                  </li>
                  {% if app.user %}
                  <li class="nav-item">
                    <a class="nav-link" href="{{path('valeur_index')}}" style="position: absolute; right: 230px; bottom :10px">
                      <span class="badge rounded-pill bg-danger">2</span><br>
                      <i style="color:white;font-size:30px;" class="bi bi-bell-fill"></i>
                    </a>
                  <li>
                        <li class="nav-item">
                          <a class="nav-link" href="{{ path('message_index') }}" style="position: absolute; right: 330px; bottom :14px">
                            <i class="bi bi-envelope-plus" class="bi bi-bell-fill" style="color:white;font-size:35px;"></i>
                          </a>
                        </li>
                        {% endif %}
                        {% if app.user %}
                          <div class="mb-3">
                            <a class="btn" style="background-color:#3965a0;color:white;position: absolute; right: 30px; " href="{{ path('app_logout') }}">
                              déconnexion
                            </a>
                          </div>
                        {% endif %}
                  </li>
                  {# <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
                    <div class="dropdown-menu">
                      <a class="dropdown-item" href="#">Action</a>
                      <a class="dropdown-item" href="#">Another action</a>
                      <a class="dropdown-item" href="#">Something else here</a>
                      <div class="dropdown-divider"></div>
                      <a class="dropdown-item" href="#">Separated link</a>
                    </div>
                  </li>
                </ul>
                <form class="d-flex">
                  <input class="form-control me-sm-2" type="text" placeholder="Search">
                  <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                </form> #}
              </div>
            </div>
          </nav>
          {% for message in app.flashes("message") %}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                {{message}}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
          {% endfor %}
          <br>
       {% block body %}{% endblock %}
       <br>
    </body>
</html>
